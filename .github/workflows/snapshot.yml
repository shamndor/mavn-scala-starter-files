name: Snapshot
on:
   push:
     branches:
       - develop
jobs:
  build:
    steps:
      - uses: actions/checkout@V2
      - name: Set Up JDK 1.8
        uses: actions/setup-java@V1
        with:
          java-version: 1.8
      - name: Build Package
        run: mvn -B verify
    

